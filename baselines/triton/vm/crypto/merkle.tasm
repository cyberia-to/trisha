// Canonical source ABI: idx deepest, d4 on top; native digest queue remains canonical.
vm_crypto_merkle__step:
    swap 4
    swap 3
    swap 1
    swap 3
    merkle_step
    swap 4
    swap 3
    swap 1
    swap 3
    return
